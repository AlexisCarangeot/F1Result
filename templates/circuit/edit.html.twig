{% extends 'admin/layout.html.twig' %}

{% block titlepage %}{{ circuit.name }}{% endblock %}

{% block body %}
    <h1>Modifier: {{ circuit.name }}</h1>

    {{ include('circuit/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn" href="{{ path('circuit_new') }}">back to list</a>

    {{ include('circuit/_delete_form.html.twig') }}
{% endblock %}
