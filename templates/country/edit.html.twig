{% extends 'admin/layout.html.twig' %}


{% block titlepage %}{{ country.name }}{% endblock %}
{% block body %}
    <div>
        <h1>Modifier: {{ country.name }}</h1>

        {{ include('country/_form.html.twig', {'button_label': 'Update'}) }}
        <a class="btn" href="{{ path('country_new') }}">back to list</a>
        {{ include('country/_delete_form.html.twig') }}
    </div>
{% endblock %}
